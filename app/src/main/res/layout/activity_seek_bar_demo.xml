<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SeekBarDemo"
    android:orientation="vertical"
    android:gravity="center">

    <SeekBar
        android:id="@+id/seek_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="30"
        android:thumb="@drawable/home"
        android:background="@drawable/custom_seek_bar"/>

<!--
android:progress="30" // it occupied 30% of seek...
android:indeterminate="true" // it show the progressing on the seek....
android:rotation="80"  // to use to rotate the seek bar
-->

    <TextView
        android:id="@+id/tv_seek_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Value : "/>

</LinearLayout>